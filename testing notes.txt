bug 
  export to excel contains the formatting strings

not a bug
  if column is a number format and is put in the rows, 
    it behaves like a text field but should behave like a number.  
    Behaviour of the standard pbi table is that a number 
    in the rows behaves as a number re sorting, 
    right alignment instead of left alignment 



bug - can't repro
  when values to rows is switched on, the show total row for ALL on/off switch is incorrect
  row totals randomly turn on when bringing in other measures or rearranging measures

set default styles
  Classic Table
  

