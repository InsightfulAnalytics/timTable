const fs = require('fs'); let f = fs.readFileSync('B:/VS Code Files/Visuals/timTable/src/visual.ts', 'utf8'); let idx = f.indexOf('// Check for conditional formatting on positive color'); let idx2 = f.indexOf('const min = minValueObj'); if (idx !== -1 && idx2 !== -1) { let newStr = '// Check for conditional formatting on data bar color\\r\\n                            if (valueColumn.objects && valueColumn.objects[i]) {\\r\\n                                const cfDataBarColor = dataViewObjects.getFillColor(\\r\\n                                    valueColumn.objects[i],\\r\\n                                    { objectName: \\x22dataBarsFormatting\\x22, propertyName: \\x22dataBarColor\\x22 }\\r\\n                                );\\r\\n                                if (cfDataBarColor) {\\r\\n                                    cellDataBarColor = cfDataBarColor;\\r\\n                                }\\r\\n                            }\\r\\n\\r\\n                            '; f = f.substring(0, idx) + newStr + f.substring(idx2); fs.writeFileSync('B:/VS Code Files/Visuals/timTable/src/visual.ts', f); console.log('success'); }
